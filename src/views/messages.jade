html
  head
  body
    ul
      - each message in messages
          li= message.text

    if channel
      form(method='post' action='/channels/' + channel)
        input(type='text' name='text' placeholder='Send a message!')
        input(type='submit' value='Submit')
